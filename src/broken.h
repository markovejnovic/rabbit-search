#include <stdlib.h>
#include <immintrin.h>

inline static char const* sz_find(char const * haystack, size_t h_length, char const* needle, size_t n_length) {
    __m256i a, b;
    a = _mm256_cmpeq_epi8(a, b);
    return 0;
}
